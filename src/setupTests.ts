// 导入 '@testing-library/jest-dom' 模块。
// 'jest-dom' 为 jest 提供一些自定义的匹配器，用于对 DOM 节点进行断言。
// 比如你可以这样做:
// expect(element).toHaveTextContent(/react/i)
// 如果想了解更多，可以访问 https://github.com/testing-library/jest-dom
import "@testing-library/jest-dom";
